# Name,   Type, SubType, Offset,   Size,    Flags
# OTA partition table for the XIAO ESP32S3 (8MB flash).
# Upstream's table allocated only the first 4MB, leaving half the chip unused
# and each OTA slot at 1.75MB - which this build filled to 95%. Each app slot
# is now 3.625MB, taking the firmware to roughly 46% of its partition.
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x3A0000,
app1,     app,  ota_1,   0x3B0000, 0x3A0000,
spiffs,   data, spiffs,  0x750000, 0xB0000,
